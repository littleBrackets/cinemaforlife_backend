const express = require("express");
const referenceController = require("../controllers/referenceController");

const router = express.Router();

// router.get("/movie-types", referenceController.getMovieTypes);
// router.get("/regions", referenceController.getRegions);
// router.get("/languages", referenceController.getLanguages);
// router.get("/professions", referenceController.getProfessions);
// router.get("/top-movies", referenceController.getTopMovies);

module.exports = router;
